# 🚀 Развёртывание mpro7.ru на Ubuntu 24

Полная инструкция по развёртыванию приложения на production сервере.

## 📋 Требования

- Ubuntu 24.04 LTS
- Домен mpro7.ru с настроенными A-записями
- Права root/sudo на сервере
- Открытые порты: 80 (HTTP), 443 (HTTPS)

## 🎯 Быстрый старт

### Автоматическое развёртывание (рекомендуется)

```bash
cd /mrpo
sudo chmod +x deploy.sh setup-ssl.sh
sudo bash deploy.sh
```

Скрипт автоматически:
- ✅ Установит Node.js (если не установлен)
- ✅ Настроит права доступа
- ✅ Установит SSL сертификат от Let's Encrypt
- ✅ Настроит nginx как reverse proxy
- ✅ Создаст и запустит systemd service
- ✅ Настроит автообновление SSL сертификата

### Ручное развёртывание

#### 1. Установка Node.js

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash -
sudo apt-get install -y nodejs
```

#### 2. Настройка прав доступа

```bash
sudo chown -R www-data:www-data /mrpo
sudo chmod +x /mrpo/Приложение\ 1/proxy.js
```

#### 3. Установка SSL сертификата

```bash
cd /mrpo
sudo chmod +x setup-ssl.sh
sudo bash setup-ssl.sh
```

#### 4. Установка systemd service

```bash
sudo cp /mrpo/mpro7.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable mpro7
sudo systemctl start mpro7
```

#### 5. Настройка nginx

```bash
sudo cp /mrpo/nginx-mpro7.conf /etc/nginx/sites-available/mpro7.ru
sudo ln -s /etc/nginx/sites-available/mpro7.ru /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

## 🔧 Управление сервисом

### Статус и логи

```bash
# Статус приложения
sudo systemctl status mpro7

# Логи приложения (live)
sudo journalctl -u mpro7 -f

# Логи nginx
sudo tail -f /var/log/nginx/mpro7-access.log
sudo tail -f /var/log/nginx/mpro7-error.log
```

### Управление

```bash
# Запуск
sudo systemctl start mpro7

# Остановка (важно: используйте перед обновлением кода)
sudo systemctl stop mpro7

# Перезапуск (быстрый способ, но не рекомендуется при обновлении кода)
sudo systemctl restart mpro7

# Перезагрузка конфигурации (без остановки сервиса)
sudo systemctl reload mpro7
```

### Остановка сервиса перед обновлением

**⚠️ ВАЖНО:** Всегда останавливайте сервис перед обновлением файлов приложения!

```bash
# Остановка приложения
sudo systemctl stop mpro7

# Проверка статуса (должно быть "inactive (dead)")
sudo systemctl status mpro7

# Проверка, что порт 5550 свободен
sudo netstat -tlnp | grep 5550
# Если команда ничего не выводит - порт свободен, можно обновлять
```

## 🔐 SSL сертификат

### Автоматическое обновление

Сертификат автоматически обновляется через systemd timer. Проверить:

```bash
sudo systemctl status certbot.timer
```

### Ручное обновление

```bash
sudo certbot renew
sudo systemctl reload nginx
```

### Проверка срока действия

```bash
sudo certbot certificates
```

## 🔥 Firewall (UFW)

Если используется UFW, откройте порты:

```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw reload
```

## 🐛 Решение проблем

### Приложение не запускается

1. Проверьте логи:
   ```bash
   sudo journalctl -u mpro7 -n 50
   ```

2. Проверьте права доступа:
   ```bash
   ls -la /mrpo/Приложение\ 1/proxy.js
   ```

3. Проверьте, что порт 5550 свободен:
   ```bash
   sudo netstat -tlnp | grep 5550
   ```

### Nginx не работает

1. Проверьте конфигурацию:
   ```bash
   sudo nginx -t
   ```

2. Проверьте логи:
   ```bash
   sudo tail -f /var/log/nginx/error.log
   ```

3. Проверьте, что приложение запущено:
   ```bash
   curl http://127.0.0.1:5550
   ```

### SSL сертификат не работает

1. Проверьте, что домен указывает на сервер:
   ```bash
   dig mpro7.ru +short
   ```

2. Проверьте сертификат:
   ```bash
   sudo certbot certificates
   ```

3. Проверьте, что порт 80 открыт:
   ```bash
   sudo ufw status
   ```

## 📊 Мониторинг

### Проверка доступности

```bash
# HTTP (должен редиректить на HTTPS)
curl -I http://mpro7.ru

# HTTPS
curl -I https://mpro7.ru
```

### Проверка SSL

```bash
openssl s_client -connect mpro7.ru:443 -servername mpro7.ru
```

## 🔄 Обновление приложения (перезаливка)

### Безопасная процедура перезаливки

При обновлении приложения важно следовать правильной последовательности, чтобы избежать простоев и проблем.

#### Шаг 1: Остановка сервиса

```bash
# Остановка приложения
sudo systemctl stop mpro7

# Проверка, что сервис остановлен
sudo systemctl status mpro7
```

**Важно:** Убедитесь, что сервис полностью остановлен перед обновлением файлов.

#### Шаг 2: Резервное копирование (рекомендуется)

```bash
# Создание резервной копии текущей версии
sudo cp -r /mrpo /mrpo.backup.$(date +%Y%m%d_%H%M%S)

# Или только критичных файлов
sudo cp -r /mrpo/Приложение\ 1 /mrpo/Приложение\ 1.backup.$(date +%Y%m%d_%H%M%S)
```

#### Шаг 3: Обновление кода

**Вариант A: Через git (если используется)**
```bash
cd /mrpo
sudo git pull origin main  # или master, или другая ветка
```

**Вариант B: Ручное обновление**
```bash
# Загрузите новые файлы в /mrpo
# Убедитесь, что все файлы на месте
```

#### Шаг 4: Настройка прав доступа

```bash
# Установка правильных прав на файлы
sudo chown -R www-data:www-data /mrpo
sudo chmod +x /mrpo/Приложение\ 1/proxy.js

# Если есть другие исполняемые файлы, добавьте права:
# sudo chmod +x /mrpo/путь/к/файлу
```

#### Шаг 5: Проверка конфигурации (если изменилась)

Если вы обновили systemd service файл:
```bash
sudo cp /mrpo/mpro7.service /etc/systemd/system/mpro7.service
sudo systemctl daemon-reload
```

Если вы обновили конфигурацию nginx:
```bash
sudo cp /mrpo/nginx-mpro7.conf /etc/nginx/sites-available/mpro7.ru
sudo nginx -t  # Проверка синтаксиса
```

#### Шаг 6: Запуск сервиса

```bash
# Запуск приложения
sudo systemctl start mpro7

# Проверка статуса
sudo systemctl status mpro7
```

#### Шаг 7: Проверка работоспособности

```bash
# 1. Проверка, что сервис запущен
sudo systemctl is-active mpro7

# 2. Проверка логов на ошибки
sudo journalctl -u mpro7 -n 50 --no-pager

# 3. Проверка доступности приложения локально
curl http://127.0.0.1:5550

# 4. Проверка через nginx (если настроен)
curl -I https://mpro7.ru

# 5. Мониторинг логов в реальном времени (на 30-60 секунд)
sudo journalctl -u mpro7 -f
# Нажмите Ctrl+C для выхода
```

#### Шаг 8: Перезагрузка nginx (если нужно)

```bash
# Если обновляли конфигурацию nginx
sudo systemctl reload nginx

# Или полный перезапуск
sudo systemctl restart nginx
```

### Быстрая перезаливка (если уверены в изменениях)

Если вы обновляете только код приложения без изменения конфигурации:

```bash
# 1. Остановка
sudo systemctl stop mpro7

# 2. Обновление кода (git pull или загрузка файлов)

# 3. Права доступа
sudo chown -R www-data:www-data /mrpo
sudo chmod +x /mrpo/Приложение\ 1/proxy.js

# 4. Запуск
sudo systemctl start mpro7

# 5. Проверка
sudo systemctl status mpro7
sudo journalctl -u mpro7 -n 20 --no-pager
```

### Откат к предыдущей версии (если что-то пошло не так)

```bash
# 1. Остановка текущей версии
sudo systemctl stop mpro7

# 2. Восстановление из резервной копии
sudo cp -r /mrpo.backup.20240101_120000/* /mrpo/

# 3. Восстановление прав
sudo chown -R www-data:www-data /mrpo
sudo chmod +x /mrpo/Приложение\ 1/proxy.js

# 4. Запуск
sudo systemctl start mpro7

# 5. Проверка
sudo systemctl status mpro7
```

### Чеклист перед перезаливкой

- [ ] Резервная копия создана
- [ ] Новые файлы проверены на наличие
- [ ] Права доступа настроены
- [ ] Конфигурационные файлы обновлены (если нужно)
- [ ] Логи проверены после запуска
- [ ] Сайт доступен и работает корректно

## 📝 Конфигурация

### Изменение порта приложения

Отредактируйте `/etc/systemd/system/mpro7.service`:

```ini
Environment="PORT=5550"
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart mpro7
```

### Изменение конфигурации nginx

Отредактируйте `/etc/nginx/sites-available/mpro7.ru`, затем:

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 📊 Проверка статуса

Быстрая проверка всех сервисов:

```bash
sudo bash /mrpo/status.sh
```

## 🎉 Готово!

После развёртывания ваш сайт будет доступен по адресу:
- **https://mpro7.ru** (основной домен)
- **https://www.mpro7.ru** (с www)

Все запросы HTTP автоматически перенаправляются на HTTPS.

---

**Поддержка:** При возникновении проблем проверьте логи и убедитесь, что все шаги выполнены правильно.

---

**P.S.** Дима кролик 🐰

