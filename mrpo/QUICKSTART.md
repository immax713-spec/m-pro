# ⚡ Быстрый старт - mpro7.ru

## 🚀 Развёртывание одной командой

```bash
cd /mrpo
sudo bash deploy.sh
```

Всё! Скрипт автоматически:
- ✅ Установит Node.js
- ✅ Настроит SSL (Let's Encrypt)
- ✅ Настроит nginx
- ✅ Запустит приложение

## 📋 Что нужно перед запуском

1. ✅ Домен mpro7.ru должен указывать на IP сервера (A-запись)
2. ✅ Порты 80 и 443 должны быть открыты в firewall
3. ✅ Права root/sudo на сервере

## 🔍 Проверка после установки

```bash
# Проверка статуса
sudo bash /mrpo/status.sh

# Или вручную
sudo systemctl status mpro7
sudo systemctl status nginx
```

## 🌐 Доступ к сайту

После установки сайт будет доступен:
- **https://mpro7.ru**

## 🆘 Если что-то пошло не так

1. Проверьте логи:
   ```bash
   sudo journalctl -u mpro7 -f
   ```

2. Проверьте nginx:
   ```bash
   sudo nginx -t
   sudo tail -f /var/log/nginx/mpro7-error.log
   ```

3. Проверьте DNS:
   ```bash
   dig mpro7.ru +short
   ```

## 📚 Подробная документация

См. [README-DEPLOY.md](README-DEPLOY.md) для детальной информации.

